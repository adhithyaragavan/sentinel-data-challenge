.env
.venv/
CLAUDE.md
HANDOFF.md
.claude/
__pycache__/
*.pyc
*.pyo
.DS_Store
pipeline_output.json
eval/results.json

# Data intelligence pipeline — local intermediates (large, regenerable)
data/
*.parquet
*.ndjson
# Cached deep-dive swarm outputs keyed by alert_id
deep_dive_cache/
# NOTE: benchmarks/results.csv and benchmarks/cpu_vs_gpu.png are the demo
# cache and ARE committed on purpose — do not ignore them.

# The hosted-dashboard demo bundle IS tracked (small, committed on purpose):
# a trimmed top-1000 queue + real swarm deep-dives, overriding the *.parquet rule.
!dashboard/demo_data/
!dashboard/demo_data/**
